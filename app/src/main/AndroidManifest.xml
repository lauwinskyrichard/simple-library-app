<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uasmcs_2301865842">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="XYZ Dictionary Application"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UASMCS_2301865842">
        <activity
            android:name=".FavoritesDetailActivity"
            android:parentActivityName=".FavoritesActivity"></activity>
        <activity
            android:name=".ExploreDetailActivity"
            android:parentActivityName=".ExploreActivity"></activity>
        <activity android:name=".ExploreActivity" />
        <activity android:name=".FavoritesActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>